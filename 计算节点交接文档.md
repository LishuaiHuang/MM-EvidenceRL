# 计算节点交接文档

更新时间：2026-09-17（Asia/Shanghai）

## 当前分支与提交

- 分支：`compute/bootstrap`
- 最新提交：`73e826a feat: assemble and train formal SFT mixture`
- 该提交已推送到远端。
- 工作区唯一未跟踪目录是 `runs/`，其中保存 TensorBoard event 文件；模型、日志和数据由 `.gitignore` 排除。

## 节点职责

本节点是计算服务器，负责模型兼容性、多模态 rollout、SFT/GRPO、显存/RSS 观察和活动 checkpoint。原始数据、图片、SQLite 搜索索引和数据预处理归数据服务器负责。

## 环境与模型

- Conda 环境：`reflectagent-grpo`
- Python：3.10.20
- PyTorch：2.9.0+cu128
- Transformers：4.57.3
- vLLM：0.11.1
- Ray：2.51.2
- PEFT：0.17.1
- 模型 snapshot：`/amax/home/lishuai/.cache/huggingface/hub/models--Qwen--Qwen2.5-VL-3B-Instruct/snapshots/66285546d2b821cf421d4f5eb2576359d3770cd3`
- 当前离线运行需设置 `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`，并传入上述本地 snapshot 路径。使用 repo id 在离线环境可能触发 Hugging Face 元数据查询。
- HF 与 vLLM 统一使用 fast processor。

## 数据位置

- 正式 manifest：`datasets/manifests/formal_sft_split.jsonl`（Git 中的小 manifest）
- 训练集：`artifacts/sft-full/formal_sft.jsonl`（本地、不进 Git）
- 图片根目录：`/amax/home/lishuai/prepared/formal`
- 搜索 SQLite：`/amax/home/lishuai/prepared/indices/formal_search/evidence.sqlite3`
- 正式 SFT 共 5,400 条，全部来自 `sft_train`：
  - Oracle：3,880
  - Teacher：1,520
  - Recovery：0
  - perception：3,780（`CROP → ANSWER`）
  - knowledge + search：1,138（`SEARCH → ANSWER`）
  - knowledge direct：482（`ANSWER`）
  - 同时包含 CROP 和 SEARCH：0

组装脚本是 `scripts/assemble_sft.py`。它按实际有效 Teacher/Recovery 去重，再用 Oracle 补足目标数，不再硬性要求 3600/1500/900；默认目标为 5,400，因为 manifest 的 `sft_train` 是 5,400，另有 600 条 `sft_dev`。

## 已完成工作

### E0 与兼容性

- 已记录服务器环境和模型 snapshot。
- 已验证 `CROP → ANSWER` 与 `SEARCH → ANSWER` 最小轨迹。
- 已验证 response-only mask、HF logprob 和 vLLM token logprob smoke。
- action-only schema：可见动作只有 `CROP`、`SEARCH`、终止 `ANSWER`；没有 `<think>`。
- 当前正式 Search evidence 主要是标题，没有正文 snippet。

### Teacher/Recovery 数据

- `scripts/teacher_recovery_rollout.py` 支持 Teacher/Recovery、GPU offset/stride、rejection 记录和可选 answer canonicalization。
- canonical Teacher 分片得到 1,520 条有效唯一 perception Teacher。
- Recovery 扫描未形成可用的正式分片；严格质量门槛下不伪造 Recovery。
- 少量真实 Recovery smoke 保存在 `artifacts/teacher-recovery-sft.jsonl`，仅用于说明/链路记录，没有进入正式集。

### 正式 SFT

脚本：`scripts/train_sft_lora.py`

- LoRA：`q_proj/k_proj/v_proj/o_proj`，rank 16，alpha 32，dropout 0.05。
- 视觉编码器冻结，基座语言模型冻结，训练 LoRA adapter。
- `gradient_checkpointing` 已开启，`enable_input_require_grads()` 已调用。
- 单样本 batch，图片按样本读取，响应 token 以 `labels=-100` 之外的区间计算 loss。
- 1 epoch、5,400 steps、学习率 `2e-4`。
- 正式输出：`artifacts/sft-lora-corrected/`
- TensorBoard：`runs/sft-lora-corrected/`
- 日志：`logs/sft-lora.log`
- tmux 会话：`mm-sft`；正式训练已经完成，当前不应期待训练进程仍在运行。
- 主要 checkpoint：`artifacts/sft-lora-corrected/checkpoint-500` 至 `checkpoint-5000`，最终 adapter 在根目录。
- 峰值观察到 GPU0 约 29.8 GiB（48 GiB 卡），未发生 OOM。

四样本演示数据另存为 `artifacts/sft-four-demo.jsonl`；使用同一脚本运行到 `/tmp/mm-evidence-sft-four-20260917`，只验证四种轨迹的序列化与 mask，不是正式 checkpoint。知识 Recovery 那条是明确标注的构造格式示例，不是通过审计的真实数据。

## 重要边界

- 不要把 Teacher 的拒绝 JSONL 当成训练样本；有效记录必须含 `trajectory`。
- 不要把 canonicalized answer 当成完全自主正确答案；若后续使用，必须保留 `answer_source=oracle_canonicalized` 元信息。
- 不要把标题当成正文事实；当前 evidence 质量有限，GRPO 可运行但 citation reward 需要明确按现有标题 evidence 定义。
- 当前 SFT 不覆盖组合路线。未来 GRPO 可以扩展到最多 2--3 个工具步并支持 `CROP + SEARCH`，无需重做全部 Oracle，但需要多轮 environment/history/evidence ledger、mask、logprob 和 reward。
- 不要在没有新需求时添加 `<think>`、正文强制重生成、深层视觉特征或全参训练。

## 下一步建议

1. 先用 `artifacts/sft-lora-corrected/` 做模型加载和离线生成检查，确认 action JSON 与 evidence_id 仍可解析。
2. 再实现最小单机 GRPO：vLLM rollout、HF learner、现有 SQLite search tool，先允许单步路线。
3. GRPO 稳定后再放开 `CROP + SEARCH` 多轮组合，并追加少量真实多轮 Teacher/Recovery。
4. 数据节点若能提供合法离线正文，再在不改变 evidence_id 的前提下给 SQLite 增加 snippet 字段并 replay 受影响样本。

## 常用命令

查看正式训练状态：

```bash
tmux ls
tail -f logs/sft-lora.log
```

查看 TensorBoard：

```bash
tensorboard --logdir runs/sft-lora-corrected --port 6006
```

重新组装正式集：

```bash
python scripts/assemble_sft.py --output artifacts/sft-full/formal_sft.jsonl
```

离线加载最终 adapter 时，使用本地 base snapshot 加载 PEFT adapter，不要将 adapter 当作完整 base model。
