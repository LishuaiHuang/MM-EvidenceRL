# MM-EvidenceRL 两台服务器协作启动说明

## 1. 当前同步内容

两台服务器都应从 GitHub 获取：

- 项目方案与实验计划；
- 根目录 `AGENTS.md`；
- 本说明文件；
- 后续提交的代码、配置、小型 manifest 和实验报告。

不要通过 Git 同步：

- 密码、SSH 私钥、API/Hugging Face Token；
- 原始数据和图片包；
- 模型权重、缓存和 checkpoint；
- 完整 rollout 日志；
- Conda/venv 环境。

## 2. 分工

### 数据节点

路径：`/opt/data/private/lishuai/projects/MM-EvidenceRL`

当前职责：

1. 只读盘点系统、Python/ML 包、磁盘和内存；
2. 记录计划使用的数据集来源、license、目录和 split key；
3. 规划 E0 所需的 40 条样本，不下载全量数据；
4. 后续负责预处理、manifest、本地检索索引和归档。

### 计算节点

当前职责：

1. 只读盘点 8 张 GPU、CUDA、Python/ML 包、磁盘和内存；
2. 验证目标 3B 模型及 processor 的单卡加载条件；
3. 核对 Transformers/vLLM/veRL 或 Ray 的兼容组合；
4. 后续负责 E0 rollout、SFT、GRPO 和性能 profiling。

## 3. 第一轮协作流程

两台服务器先各自建立独立分支：

```bash
# 数据节点
git switch -c data/bootstrap

# 计算节点
git switch -c compute/bootstrap
```

若分支已经存在，使用 `git switch <branch>`，不要重复创建。

第一轮只生成两份互不冲突的报告：

```text
docs/server_profiles/data-node.md
docs/server_profiles/compute-node.md
```

报告记录命令输出和结论，不记录密码、Token、私钥、完整环境变量或其他凭据。两端分别提交并推送自己的分支，之后统一 review，再决定依赖安装方案。

## 4. 给数据节点 Codex CLI 的第一条任务

```text
阅读根目录 AGENTS.md 和 MM-EvidenceRL_实验计划.md。你负责数据节点，只做只读环境盘点，不下载数据、不安装或升级依赖、不实现训练代码。运行 pwd、git status、df -h、free -h、nvidia-smi（若可用）、python3 --version，并检查 torch、transformers、vllm、ray、deepspeed、datasets 是否已安装及其版本。不要输出环境变量或任何凭据。把结果和对 E0 数据准备的最小建议写入 docs/server_profiles/data-node.md，在 data/bootstrap 分支提交并推送。若仓库存在未提交改动，不覆盖它们，先报告。
```

## 5. 给计算节点 Codex CLI 的第一条任务

```text
阅读根目录 AGENTS.md 和 MM-EvidenceRL_实验计划.md。你负责 8x48GB 计算节点，只做只读环境盘点，不下载模型或数据、不安装或升级依赖、不编写训练代码。运行 pwd、git status、df -h、free -h、nvidia-smi、python3 --version，并检查 torch、transformers、vllm、ray、deepspeed、datasets 是否已安装及其版本。不要输出环境变量或任何凭据。重点记录 GPU 型号/拓扑、CUDA/driver、当前可用磁盘，以及多模态 rollout 依赖现状。把结果和 E0 兼容性风险写入 docs/server_profiles/compute-node.md，在 compute/bootstrap 分支提交并推送。若仓库存在未提交改动，不覆盖它们，先报告。
```

## 6. 第一轮完成后的决策

拿到两份报告后再统一决定：

- Python 与 PyTorch 基线版本；
- Qwen2.5-VL-3B 的具体 model/processor revision；
- vLLM/veRL 原生路径是否值得先试；
- 是否直接采用 full re-prefill；
- E0 小数据由哪个公开数据子集组成；
- 模型缓存、运行数据和 active checkpoint 的具体绝对路径。

在这些项确定前，不让两个 Codex 同时开始写环境代码，也不让数据节点提前下载全量 benchmark。
